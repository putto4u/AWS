AWS를 CLI(Command Line Interface) 환경에서 제어하기 위해서는 **윈도우 터미널(Windows Terminal)** 환경을 구축하는 것이 효율적입니다. 

  
단순히 명령어를 입력하는 창을 넘어, 여러 탭을 지원하고 가독성이 뛰어난 현대적인 개발 환경을 구성해 보겠습니다.

---

## 1. 윈도우 터미널과 AWS CLI 설치

Windows에서 AWS를 관리하려면 우선 도구들을 설치해야 합니다.

### [Step 1] 윈도우 터미널 설치



 
   
<img width="500" height="987" alt="image" src="https://github.com/user-attachments/assets/9ac54b24-17e2-4768-bece-1e2317997f06" />  
---  


  


    
Windows 10/11 사용자라면 기본적으로 설치되어 있거나 Microsoft Store에서 무료로 다운로드할 수 있습니다.

* 기존 `cmd.exe`나 `PowerShell`보다 탭 기능, 테마 설정, 폰트 렌더링이 월등히 뛰어납니다.
---

### [Step 2] AWS CLI 설치

<img width="500" height="1085" alt="image" src="https://github.com/user-attachments/assets/630193a9-68c6-46c4-8396-5c36787c017f" />


---
**프로그램 설치후 아이콘이나 프로그램이 보이지 않습니다. 
윈도우 터미널 명령줄에서 AWS 리소스를 관리하기 위한 공식 도구입니다.**

1. [AWS CLI 공식 페이지](https://aws.amazon.com/ko/cli/)에서 Windows용 MSI 설치 프로그램을 다운로드합니다.
2. 설치 완료 후 윈도우 터미널을 열고 다음 명령어를 입력하여 설치 여부를 확인합니다.
```bash
aws --version

```


* **과금 주의:** CLI 설치 자체는 **무료**입니다. 하지만 CLI를 통해 인스턴스를 생성하거나 유료 API를 호출하면 비용이 청구되므로 명령어 실행 시 주의가 필요합니다.



---

## 2. IAM 사용자 자격 증명 설정 (Configure)

앞서 생성한 IAM 사용자의 권한을 윈도우 터미널에 연결해야 합니다. 이때 **액세스 키(Access Key ID)**와 **비밀 액세스 키(Secret Access Key)**가 필요합니다.

### 구성 명령어 실행

터미널에 아래 명령어를 입력하고 정보를 기입합니다.



  <img width="500" height="469" alt="image" src="https://github.com/user-attachments/assets/9a888ee1-b1da-446b-8bab-68e93dcbd6fc" />  

  
  

```bash
aws configure

```

1. **AWS Access Key ID:** IAM 사용자의 액세스 키 입력
2. **AWS Secret Access Key:** IAM 사용자의 비밀 키 입력
3. **Default region name:** `ap-northeast-2` (서울 리전)
4. **Default output format:** `json` (기본값)

---

## 3. 터미널 환경 최적화 (가독성 설정)

교재를 따라 실습할 때 명령어가 한눈에 들어오도록 설정을 변경하는 것이 좋습니다.

* **JSON 뷰어 활용:** AWS 응답 결과는 JSON 형태입니다. `jq`와 같은 도구를 설치하면 데이터 필터링이 쉬워집니다.
* **프로필 별칭(Alias) 설정:** 자주 사용하는 명령어를 단축어로 등록할 수 있습니다. (예: `ls` 대신 `aws s3 ls`)

---

## 4. 보안 및 비용 관리 수칙

윈도우 터미널을 사용할 때 가장 주의해야 할 점은 **자격 증명 유출**입니다.

* **자격 증명 파일 보안:** `aws configure`를 마치면 사용자의 홈 디렉토리(`.aws/credentials`)에 평문으로 키가 저장됩니다. 이 파일을 절대 외부에 노출하거나 GitHub에 업로드하지 마십시오.
* **권한 제한:** 본인 계정이라 하더라도 터미널용 IAM 사용자에게는 필요한 최소한의 권한만 부여하는 것이 과금 사고를 막는 지름길입니다.
* **실시간 과금 확인:** CLI를 통해 리소스를 대량 생성하는 스크립트를 실행할 경우, 즉시 [Billing] 콘솔에서 예상 요금을 체크하는 습관을 들여야 합니다.

---

AWS CLI 자격 증명(Access Key, Secret Key) 정보는 특정 경로의 파일에 저장됩니다. 사무실이나 학교에서 설정한 환경을 집이나 다른 PC에서도 동일하게 사용하려면, USB에 저장한 파일을 학원 PC의 **사용자 홈 디렉토리** 내 특정 폴더에 복사해야 합니다.

---

## 1. 자격 증명 파일의 저장 위치

AWS CLI는 `aws configure` 명령을 실행할 때 입력한 정보를 다음 경로에 자동으로 생성하고 저장합니다.

### Windows 기준 경로

`C:\Users\[사용자계정명]\.aws\`

이 폴더 안에는 보통 두 개의 파일이 들어 있습니다.

1. **credentials:** 액세스 키(Access Key ID)와 비밀 액세스 키(Secret Access Key)가 들어 있는 핵심 보안 파일입니다.
2. **config:** 기본 리전(예: ap-northeast-2)과 출력 형식(json) 정보가 들어 있습니다.

---

## 2. USB 저장 및 학원 PC 복사 절차 (GitHub 실습 포맷)

### [Step 1] 집에서 준비 (Export)

1. 내 PC의 `C:\Users\[본인계정]\.aws` 폴더로 이동합니다.
2. `.aws` 폴더 전체를 USB에 복사합니다.
* **주의:** `.aws` 폴더가 보이지 않는다면 폴더 옵션에서 '숨김 항목 표시'를 체크하세요.



### [Step 2] 학원 PC에서 설정 (Import)

1. 학원 PC의 윈도우 터미널(또는 CMD)을 엽니다.
2. 현재 로그인된 사용자 이름을 확인하기 위해 `echo %USERPROFILE%` 명령어를 입력해 봅니다.
3. 확인된 경로(예: `C:\Users\Student`)로 이동하여 **USB에 담아온 `.aws` 폴더를 그대로 복사**합니다.
* 결과적으로 `C:\Users\Student\.aws\credentials` 형태가 되어야 합니다.



---

## 3. 정상 작동 확인

파일 복사가 완료되었다면, 학원 PC의 터미널에서 다음 명령어를 입력하여 내 IAM 계정 정보가 올바르게 인식되는지 확인합니다.

```bash
aws sts get-caller-identity

```

* **결과:** 본인의 AWS 계정 ID와 IAM 사용자 이름이 화면에 출력되면 성공입니다.

---

## 4. 유료 전환 및 보안 주의사항

* **보안 위험:** `credentials` 파일은 암호화되지 않은 평문입니다. 강의가 끝난 후 학원 PC에서 이 파일을 **반드시 삭제**해야 합니다. 삭제하지 않으면 다음 사용자가 내 계정 권한을 사용하여 리소스를 생성하고 **나에게 과금**이 발생하게 됩니다.
* **USB 분실 주의:** USB를 분실할 경우 누구나 내 AWS 관리자 권한을 가질 수 있습니다. 가급적이면 USB 보관에 유의하거나, 키 유출 시 즉시 AWS 콘솔에서 해당 액세스 키를 '비활성화(Deactivate)' 하십시오.
* **API 호출 비용:** `get-caller-identity`와 같은 단순 조회 명령어는 무료입니다. 하지만 복사된 키를 이용해 실제 자원(EC2 등)을 생성하면 비용이 발생합니다.

---

Next Step: 학원 실습 종료 후 자격 증명 파일 안전하게 삭제하는 방법
