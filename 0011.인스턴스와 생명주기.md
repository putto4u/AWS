## 클라우드 인스턴스(Cloud Instance)

클라우드 컴퓨팅 환경에서 **인스턴스(Instance)**는 가상화 기술을 통해 생성된 독립적인 컴퓨팅 자원을 의미합니다. 물리적인 서버 하드웨어를 직접 소유하거나 관리하지 않고도, 네트워크를 통해 필요한 만큼의 CPU, 메모리, 저장 공간을 갖춘 가상 서버를 즉시 할당받아 사용할 수 있습니다.

---

### 1. 가상 머신(VM)과 인스턴스의 차이

일반적으로 **가상 머신(Virtual Machine)**과 **인스턴스**는 혼용되어 사용되지만, 클라우드 맥락에서는 다음과 같은 미묘한 차이가 있습니다.

* **가상 머신 (VM):** 하이퍼바이저(Hypervisor) 기술을 통해 물리적 서버 위에 생성된 가상의 컴퓨터 그 자체를 지칭하는 기술적 용어입니다.
* **인스턴스 (Instance):** 클라우드 서비스 환경(AWS, Azure, GCP 등)에서 실행 중인 가상 머신을 의미합니다. 즉, 특정 템플릿(이미지)을 바탕으로 클라우드 인프라 위에 '실체화(Instantiate)'된 상태를 강조합니다.

---

### 2. 인스턴스의 주요 특징

* **즉각적인 프로비저닝 (Elasticity):** 물리적 서버 설치에는 수일에서 수주가 소요되지만, 클라우드 인스턴스는 단 몇 분 만에 생성 및 삭제가 가능합니다.
* **유연한 확장성 (Scalability):** 트래픽 증가 시 인스턴스의 사양을 높이거나(Scale-up), 인스턴스의 개수를 늘려(Scale-out) 대응할 수 있습니다.
* **종량제 요금제 (Pay-as-you-go):** 사용한 시간과 자원량만큼만 비용을 지불하므로 초기 인프라 투자 비용을 절감할 수 있습니다. **[유료 과금 주의]**

---

### 3. 클라우드 인스턴스의 핵심 구성 요소

교재 학습 시 인스턴스를 단순한 '서버'가 아닌 다음 요소들의 결합체로 이해해야 합니다.

| 구성 요소 | 설명 | 비고 |
| --- | --- | --- |
| **이미지 (Image)** | OS와 기본 소프트웨어가 설치된 상태의 스냅샷 (예: AMI) | 서버의 붕어빵 틀 역할 |
| **컴퓨팅 사양** | CPU(vCPU), RAM(Memory)의 크기 결정 | 인스턴스 유형 선택 |
| **스토리지 (Storage)** | OS가 설치되거나 데이터를 저장하는 가상 디스크 | **[유료 과금 주의]** |
| **네트워크 (Network)** | 가설 사설망(VPC), 서브넷, 공인/사설 IP 설정 | 외부 통신 경로 결정 |
| **보안 (Security)** | 인바운드/아웃바운드 트래픽을 제어하는 가상 방화벽 | 보안 그룹 설정 |

---

### 4. 인스턴스 유형 선택 가이드

워크로드의 특성에 따라 최적의 인스턴스를 선택하는 것이 효율적인 클라우드 운영의 핵심입니다.

* **일반 웹 서버:** 범용 인스턴스 (Balanced CPU/Memory)
* **데이터 분석 / 머신러닝:** 가속 컴퓨팅 (GPU 탑재) 또는 메모리 최적화 인스턴스
* **고성능 웹 애플리케이션:** 컴퓨팅 최적화 (High vCPU)
* **개발 및 테스트용:** 공유 자원을 사용하는 저비용 인스턴스 (예: t 시리즈)

> **[유료 전환 및 과금 주의]**
> 클라우드 인스턴스는 생성하는 순간부터 시간당 비용이 발생합니다. 실습 종료 후에는 반드시 인스턴스를 **'종료(Terminate)'** 상태로 변경해야 추가 과금을 방지할 수 있습니다. 단순히 '중지(Stop)' 상태로 두면 CPU 비용은 청구되지 않으나, 연결된 **스토리지(EBS) 비용은 계속 청구**됩니다.

Next Step: **클라우드 인스턴스 생명 주기와 상태 관리**

---
인스턴스 생명 주기(Life Cycle)는 가상 서버가 생성되는 순간부터 완전히 삭제될 때까지 거치는 모든 상태 변화를 의미합니다. 각 상태에 따라 서비스 가용 여부와 **과금 발생 여부**가 달라지므로 이를 정확히 관리하는 것이 중요합니다.

---

## 클라우드 인스턴스 생명 주기 주요 상태

인스턴스는 관리자의 명령이나 시스템 설정에 따라 다음과 같은 상태로 전환됩니다.

### 1. Pending (대기 중)

* **설명:** 인스턴스 생성을 요청한 후, 클라우드 제공업체가 가상 머신을 준비하고 네트워크 설정을 구성하는 단계입니다.
* **상태:** 부팅 전 단계이며 서비스 이용이 불가능합니다.
* **비용:** 과금이 발생하지 않습니다.

### 2. Running (실행 중)

* **설명:** 운영체제가 부팅되어 정상적으로 가동 중인 상태입니다. 사용자는 SSH나 RDP를 통해 접속할 수 있습니다.
* **상태:** 서비스 운영 가능.
* **비용:** **[유료]** 인스턴스 유형에 따른 시간당 비용이 청구됩니다.

### 3. Stopping / Stopped (중지 중 / 중지됨)

* **설명:** 사용자가 인스턴스를 종료(Shut down)한 상태입니다. 데이터는 스토리지(EBS 등)에 유지되지만 CPU와 RAM 자원은 반납됩니다.
* **상태:** 접속 불가.
* **비용:** 인스턴스 비용은 청구되지 않으나, 연결된 **스토리지(EBS) 및 탄력적 IP(EIP) 비용은 계속 청구**됩니다.

### 4. Shutting-down / Terminated (종료 중 / 종료됨)

* **설명:** 인스턴스를 영구적으로 삭제하는 상태입니다. 이 상태가 되면 다시 시작할 수 없으며 연결된 데이터도 삭제(설정에 따라 다름)됩니다.
* **상태:** 인스턴스 소멸.
* **비용:** 모든 인스턴스 과금이 중단됩니다.

---

## 상태별 과금 및 데이터 유지 요약표

| 상태 | 서비스 이용 | 인스턴스 과금 | 스토리지(EBS) 과금 | 데이터 유지 |
| --- | --- | --- | --- | --- |
| **Pending** | X | 미과금 | 과금 | 유지 |
| **Running** | **O** | **[유료]** | **[유료]** | 유지 |
| **Stopped** | X | 미과금 | **[유료]** | 유지 |
| **Terminated** | X | 미과금 | 미과금 | **삭제** |

---

## 효율적인 생명 주기 관리 팁

* **자동 중지 설정:** 개발 환경 등 야간에 사용하지 않는 서버는 스케줄링을 통해 자동으로 **Stopped** 상태로 전환하여 비용을 절감합니다.
* **종료 보호(Termination Protection):** 실수로 인스턴스를 삭제(Terminated)하는 것을 방지하기 위해 중요 서버에는 종료 보호 옵션을 활성화합니다.
* **스냅샷 활용:** 인스턴스를 종료(Terminated)하기 전, 나중에 다시 구성할 필요가 있다면 스냅샷을 찍어 이미지를 보관합니다. **(주의: 스냅샷 저장 비용 발생)**

---

Next Level : 클라우드 보안 모델 및 책임 공유 모델(Shared Responsibility Model)

Pending, Running, Stopped, Terminated, Instance Life Cycle, Snapshot
