AWS에서 EC2 인스턴스를 수동(Console)이 아닌 **IaC(Infrastructure as Code)**로 생성하는 것은 인프라의 일관성을 유지하고 자동화하기 위한 필수 단계입니다. 가장 대표적인 도구인 **Terraform**과 **AWS CloudFormation**을 이용한 방법을 비교하여 설명합니다.

---

## 1. Terraform을 이용한 EC2 생성 (가장 대중적인 IaC)

테라폼은 선언적 언어(HCL)를 사용하며, 멀티 클라우드 지원과 직관적인 문법 덕분에 현재 업계 표준으로 자리 잡고 있습니다.

### 필수 구성 요소 코드 (main.tf)

EC2를 만들 때 반드시 필요한 **[AMI, 인스턴스 타입, 네트워크(Subnet), 보안 그룹]**을 정의합니다.

```hcl
# 1. 공급자(AWS) 설정
provider "aws" {
  region = "ap-northeast-2" # 서울 리전
}

# 2. 보안 그룹 생성 (SSH 접속 허용)
resource "aws_security_group" "web_sg" {
  name = "allow_ssh_web"
  ingress {
    from_port   = 22
    to_port     = 22
    protocol    = "tcp"
    cidr_blocks = ["0.0.0.0/0"]
  }
}

# 3. EC2 인스턴스 정의
resource "aws_instance" "enterprise_server" {
  ami           = "ami-042e76978ade33461" # Ubuntu 24.04 LTS (서울 리전 기준)
  instance_type = "t3.medium"             # 성능에 따른 선택

  vpc_security_group_ids = [aws_security_group.web_sg.id]
  key_name               = "my-key-pair"  # 미리 생성된 키페어 이름

  tags = {
    Name = "Enterprise-Master-Node"
  }
}

```

### 실행 프로세스

1. **`terraform init`**: 공급자 플러그인 설치
2. **`terraform plan`**: 생성될 리소스 미리 보기 (드라이 런)
3. **`terraform apply`**: 실제 AWS에 리소스 생성

---

## 2. AWS CloudFormation (AWS 네이티브 도구)

AWS에서 제공하는 JSON/YAML 기반의 서비스입니다. 별도의 설치 없이 AWS 콘솔 내에서 바로 실행 가능하다는 장점이 있습니다.

### YAML 템플릿 예시

```yaml
Resources:
  MyEC2Instance:
    Type: 'AWS::EC2::Instance'
    Properties:
      ImageId: 'ami-042e76978ade33461'
      InstanceType: 't3.medium'
      KeyName: 'my-key-pair'
      SecurityGroups:
        - !Ref WebSecurityGroup
  WebSecurityGroup:
    Type: 'AWS::EC2::SecurityGroup'
    Properties:
      GroupDescription: Allow SSH access
      SecurityGroupIngress:
        - IpProtocol: tcp
          FromPort: 22
          ToPort: 22
          CidrIp: 0.0.0.0/0

```

---

## 3. IaC 사용 시 실전 팁 (IT 전문가 권장)

* **변수화(Variables):** AMI ID나 인스턴스 타입을 코드에 직접 적지 말고, 변수 파일로 분리하여 환경(Dev/Prod)에 따라 다르게 주입하세요.
* **사용자 데이터(User Data):** EC2가 생성되자마자 도커(Docker)를 설치하거나 앞서 만든 **Dockerfile을 빌드/실행**하도록 `user_data` 섹션에 스크립트를 포함할 수 있습니다.
* **상태 관리(State Management):** 테라폼 사용 시 `terraform.tfstate` 파일을 S3 같은 원격 저장소에 보관하여 팀원들과 형상을 동기화하세요.

---

## 4. 자주 실수하는 부분

* **가용 영역(AZ) 불일치:** 서브넷은 `ap-northeast-2a`인데 볼륨이나 다른 리소스가 `2c`에 있으면 연결되지 않습니다.
* **권한 부족:** IaC를 실행하는 IAM 계정에 `AmazonEC2FullAccess` 등 적절한 권한이 있는지 반드시 확인해야 합니다.
* **기본 VPC 활용:** 별도의 VPC 코드를 짜지 않으면 'Default VPC'에 생성되는데, 엔터프라이즈 환경에서는 보안을 위해 커스텀 VPC를 먼저 정의하고 그 안에 EC2를 배치하는 것이 정석입니다.

---

**참고 출처 및 사이트**

* [Terraform AWS Provider Documentation](https://registry.terraform.io/providers/hashicorp/aws/latest/docs)
* [AWS CloudFormation User Guide](https://docs.aws.amazon.com/AWSCloudFormation/latest/UserGuide/Welcome.html)

현재 환경에서 바로 실행해 보실 수 있도록, **작성하신 Dockerfile을 EC2 생성 시 자동으로 빌드하여 컨테이너로 띄워주는 테라폼 풀 코드를 구성해 드릴까요?**
