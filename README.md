# AWS
AWS Lect Book
---
## 💸 AWS 실습 환경(EC2, RDS, S3) 폐기 시 요금 및 과금 방지 가이드

스터디를 위한 실습에 있어서 **AWS 프리 티어(Free Tier)를 활용**하고 **단시간(수 시간)** 동안만 사용한다면, **요금은 거의 0원**에 수렴하거나 발생하지 않습니다. AWS는 대부분의 서비스를 초/분 단위로 과금하며, 소규모 리소스는 1년 동안 무료 사용량이 제공되기 때문입니다.

그러나 예상치 못한 과금은 **사용 후 리소스를 완전히 삭제하지 않고 방치**했을 때 발생합니다. 특히 **스토리지(EBS, RDS Snapshot, S3 객체)**는 인스턴스를 중지해도 계속 요금이 부과되므로, 다음 가이드에 따라 **3가지 핵심 리소스를 완전 삭제**해야 합니다.

---

## 1. 🚨 과금 방지를 위한 3대 원칙 (핵심)

| 원칙 | 서비스 | 세부 내용 |
| :--- | :--- | :--- |
| **❌ 종료(Stop) 금지** | EC2, RDS | 인스턴스를 **정지(Stop)**해도 **스토리지 요금**은 계속 발생합니다. 반드시 **Terminate(폐기)** 또는 **Delete(삭제)** 해야 합니다. |
| **🗑️ 3대 저장소 삭제** | EBS, RDS Snapshot, S3 | 컴퓨팅 자원(VM)을 삭제해도 **저장 공간**은 남습니다. 반드시 해당 콘솔에서 수동으로 확인하고 삭제해야 합니다. |
| **IP 주소 해제** | EIP | 사용 중인 EC2에 **연결되지 않은** **탄력적 IP(EIP)**는 방치 시 요금이 부과됩니다. 반드시 **릴리즈(Release)**해야 합니다. |

---

## 2. 💻 서비스별 과금 방지 완벽 가이드

### A. 웹 서버 및 VM (EC2 인스턴스)

EC2 인스턴스 자체보다 **연결된 EBS 볼륨**과 **EIP** 때문에 요금이 발생하는 경우가 가장 많습니다.
* Amazon EC2: 프리티어는 t2.micro(또는 특정 리전의 t3.micro)만 지원합니다. 
**실수로 t2.medium 등을 선택하면 즉시 과금됩니다.** 
또한 월 750시간은 인스턴스 1대를 한 달 내내 켜두는 기준이므로, 2대를 동시에 켜두면 보름 만에 유료로 전환됩니다.

| 단계 | 작업 콘솔 | 필수 조치 | 실전 팁 |
| :--- | :--- | :--- | :--- |
| **1단계: 인스턴스 폐기** | EC2 → 인스턴스 | 해당 인스턴스 선택 후, **작업(Actions) → 인스턴스 상태 → 종료(Terminate)** | **중지(Stop)** 버튼과 헷갈리지 않도록 주의. `Terminated` 상태가 되어야 과금이 멈춥니다. |
| **2단계: EBS 볼륨 확인 및 삭제** | EC2 → EBS → 볼륨 | 인스턴스 종료 시 **자동 삭제되지 않은** 볼륨을 확인하고 수동으로 삭제. | 실습 환경에서 EC2 생성 시 **'종료 시 삭제' 옵션**을 체크했는지 확인하면 편리합니다. |
| **3단계: 탄력적 IP 해제** | EC2 → 네트워크 및 보안 → 탄력적 IP | 사용하지 않는 IP 주소를 선택하고, **작업 → 주소 릴리즈(Release)** | EIP는 **'연결되지 않은'** 상태로 방치될 경우 시간당 약 $0.005$의 요금이 발생합니다. |

---

### B. DB 서버 (RDS)

RDS는 관리형 서비스이므로 EC2보다 삭제 절차가 더 까다롭습니다. **자동 백업**과 **최종 스냅샷** 때문에 요금이 남을 수 있습니다.
**Amazon RDS: EC2와 마찬가지로 db.t2.micro 등 특정 사양만 무료입니다.** 
특히 '다중 AZ(Multi-AZ)' 기능을 활성화하면 프리티어 대상에서 제외되어 고액의 비용이 청구될 수 있습니다.

| 단계 | 작업 콘솔 | 필수 조치 | 실전 팁 |
| :--- | :--- | :--- | :--- |
| **1단계: DB 인스턴스 삭제** | RDS → 데이터베이스 | 해당 DB 인스턴스 선택 후, **작업 → 삭제(Delete)** | **DB 인스턴스 삭제 시** **"최종 스냅샷 생성"** 옵션을 반드시 **'아니요'**로 선택해야 추가 저장 비용이 발생하지 않습니다. |
| **2단계: DB 스냅샷 삭제** | RDS → 스냅샷 | 1단계에서 실수로 최종 스냅샷을 생성했다면, 해당 **스냅샷을 수동으로 삭제**해야 합니다. | 자동 백업 기능은 인스턴스 삭제 시 백업 기간에 따라 자동으로 삭제되지만, 수동으로 만든 스냅샷(Manual Snapshot)은 남아있을 수 있습니다. |
| **3단계: DB 서브넷 그룹 삭제** | RDS → 서브넷 그룹 | 실습용으로 생성한 서브넷 그룹은 삭제 가능하지만, 삭제하지 않아도 요금은 발생하지 않습니다. (선택 사항) | |

---

### C. 파일 저장소 (S3 버킷)

S3는 객체 스토리지 자체의 저장 용량(GB당 요금)과 요청 수(Request 요금)로 과금됩니다.
공간을 차지하거나 데이터를 외부로 보낼 때 발생하는 비용입니다.

Amazon S3: 저장 용량(5GB)은 적지만, PUT, COPY, POST 등 API 요청 횟수가 많아지면 과금됩니다.

EBS (Elastic Block Store): EC2 인스턴스를 삭제해도 연결된 EBS 볼륨(디스크)을 삭제하지 않으면 보관 비용이 계속 발생합니다. 프리티어는 총 30GB까지만 제공합니다.

Data Transfer Out: AWS 내부로 들어오는 데이터(Inbound)는 무료지만, AWS에서 외부로 나가는 데이터(Outbound)는 월 100GB 초과 시 비용이 발생합니다.

| 단계 | 작업 콘솔 | 필수 조치 | 실전 팁 |
| :--- | :--- | :--- | :--- |
| **1단계: 버킷 내용물 비우기** | S3 → 버킷 | 삭제할 버킷 내부의 **모든 객체**를 먼저 **영구 삭제**해야 합니다. | 버킷 자체를 바로 삭제할 수 없으며, 반드시 **'버킷 비우기(Empty)'** 기능을 사용해야 합니다. |
| **2단계: 버킷 삭제** | S3 → 버킷 | 내용물이 비워진 것을 확인한 후, 버킷을 선택하고 **삭제(Delete)**합니다. | S3는 **Versioning(버전 관리)** 기능이 켜져 있으면, 비워도 이전 버전의 객체가 남아있을 수 있습니다. **Versioning 상태를 확인**하고 모든 버전을 영구 삭제해야 합니다. |
| **3단계: 불완전 멀티파트 업로드 확인** | S3 → 버킷 → 관리 탭 | 대용량 파일 업로드 중 중단된 **'불완전 멀티파트 업로드'** 데이터가 남아있지 않은지 확인하고 삭제해야 합니다. | 이 데이터는 일반 객체 목록에 보이지 않지만 스토리지 요금을 발생시킵니다. |

---

3. 관리 및 보안 (고급 기능 활성화)
설정 한 번으로 지속적인 비용을 발생시키는 서비스들입니다.

AWS Directory Service: Managed Microsoft AD 등은 무료 평가판 기간이 짧거나 아예 프리티어 대상이 아닐 수 있습니다.

AWS Config: 리소스 변경 사항을 기록하는 서비스로, 리소스가 많을수록 기록되는 '구성 항목'당 비용이 산정되어 예상치 못한 청구서를 받게 됩니다.

NAT Gateway: VPC 내의 프라이빗 서브넷에서 외부 인터넷으로 나가기 위해 설정하지만, 시간당 사용료와 데이터 처리 비용이 모두 부과되는 유료 서비스입니다.
---

## 🛠️ IT 전문가 팁: 비용 모니터링 자동화

수업 또는 실습 후 실수로 리소스를 남기지 않도록, AWS에서 제공하는 비용 알림 기능을 활용하여 **예산(Budget)**을 설정하는 것이 가장 효과적입니다.

1.  **AWS Budgets 설정:** AWS Billing 콘솔에서 **Budgets** 메뉴로 이동합니다.
2.  **예산 생성:** 월별 예산 목표액을 **$1** 등으로 낮게 설정합니다.
3.  **알림 설정:** 실제 지출액이 이 예산의 **50%**나 **80%**에 도달하면 이메일이나 SMS로 알림이 오도록 설정하여, 예상치 못한 과금이 시작될 때 즉시 인지하고 대처할 수 있습니다.

이 과정을 통해 실습에 집중하고 종료 후에도 비용 걱정 없이 깔끔하게 환경을 정리할 수 있습니다.
